#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <fcntl.h>
#include <termios.h>

#define BAUDRATE_GPS B115200



int open_gps(char const *port_name, int data_size);
int open_gps2(char const *port_name, int data_size);
bool read_gps(int port, float &lon, float &lat);
